CREATE TABLE Users (
    id INT AUTO_INCREMENT,
    username TEXT NOT NULL,
    password TEXT NOT NULL,
    phone_number VARCHAR(15) NOT NULL,
    email TEXT NOT NULL,
    created_at DATETIME,
    PRIMARY KEY (id)
);

CREATE TABLE 